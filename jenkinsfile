


   stage('checkout') {
       steps {
            git credentialsId: 'jenkins', 
                url: 'git@git.xxx.com:jianjing/jenkinspipeline.git',
                branch: "test" 
        }
    }

    stage('test') {
        steps {
            sh "echo ${GIT_BRANCH}"
        }
    }       
