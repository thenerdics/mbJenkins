node {
    stage('test3') {
        echo env.GIT_BRANCH
        if (env.GIT_BRANCH == 'afterTest') {
            echo ${env.GIT_BRANCH}
        } else {
            echo 'I execute elsewhere'
        }                        
    }        
}
