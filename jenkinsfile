node {
    stage('test3') {
        echo GIT_BRANCH
        if (GIT_BRANCH == 'afterTest') {
            echo ${GIT_BRANCH}
        } else {
            echo 'I execute elsewhere'
        }                        
    }        
}
