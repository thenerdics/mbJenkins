node {
    stage('test3') {
        echo env.BRANCH_NAME
        if (env.BRANCH_NAME == 'afterTest') {
            echo ${env.BRANCH_NAME}
        } else {
            echo 'I execute elsewhere'
        }                        
    }        
}
