pipeline {
    agent any
    stages {
        stage('example') {
            steps {
                
                message=$( curl localhost:8080 )
                echo "Hello $message"
                
            }
        }
    }
    post {
        always {
            echo "Always post this"
        }
    }
}