pipeline {
    agent any
    stages {
        stage('example') {
            steps {
                sh '''
                message=$( curl 'http://localhost:8080' )
                echo "Hello $message"
                '''
                
            }
        }
    }
    post {
        always {
            echo "Always post this"
        }
    }
}